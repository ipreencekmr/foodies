'use client';

export default function Error({ error }) {
    return <main className="error">
        <h1 className="error">An error occured!</h1>
        <p>Failed to share the meal</p>
    </main>
}